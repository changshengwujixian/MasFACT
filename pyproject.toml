[project]
name = "masfact-core"
version = "0.1.0"
description = "Core technical implementation of MasFACT for continual MAS topology transfer"
requires-python = ">=3.10"
dependencies = ["torch>=2.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["masfact*"]

[project.optional-dependencies]
protocol = ["datasets>=2.18"]
